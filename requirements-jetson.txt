# Jetson Nano JetPack 4.6 (L4T R32.x) — Python 3.6
# PyTorch is installed separately from the NVIDIA wheel (see scripts/jetson_setup.sh)
numpy>=1.17.3,<1.20
pandas>=1.3,<2.0
ta>=0.11
scikit-learn>=0.24,<1.0
requests>=2.25
python-dotenv>=0.17
tqdm>=4.60
# google-generativeai does not support Python 3.6; commentary falls back to rule-based templates
